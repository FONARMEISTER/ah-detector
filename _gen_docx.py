"""Generate project description docx (НИР сопроводительная записка)."""

from docx import Document
from docx.shared import Pt, Cm
from docx.enum.text import WD_ALIGN_PARAGRAPH

doc = Document()

style = doc.styles["Normal"]
font = style.font
font.name = "Times New Roman"
font.size = Pt(14)
style.paragraph_format.space_after = Pt(6)
style.paragraph_format.line_spacing = 1.15

# ── Title ─────────────────────────────────────────────────────────────────────

title = doc.add_paragraph()
title.alignment = WD_ALIGN_PARAGRAPH.CENTER
run = title.add_run(
    "Мультимодальная классификация поведенческой амбивалентности/колебания "
    "на основе позднего слияния текстовых, аудио- и видеопризнаков"
)
run.bold = True
run.font.size = Pt(16)

doc.add_paragraph()

meta = doc.add_paragraph()
meta.add_run("Тематика: ").bold = True
meta.add_run("Multimodal Deep Learning / Affective Computing.")

meta2 = doc.add_paragraph()
meta2.add_run("Направление: ").bold = True
meta2.add_run("Распознавание поведенческих паттернов в видеозаписях.")

doc.add_paragraph()

# ── Актуальность ──────────────────────────────────────────────────────────────

doc.add_heading("Актуальность", level=1)

doc.add_paragraph(
    "Поведенческая амбивалентность/колебание (Behavioral Ambivalence/Hesitancy, A/H) "
    "проявляется в ситуациях, когда человек демонстрирует противоречивые сигналы "
    "через речь, голос и язык тела. Автоматическое распознавание A/H востребовано "
    "в клинической психологии, анализе переговоров и исследованиях коммуникации."
)

doc.add_paragraph(
    "Основная сложность задачи состоит в том, что A/H выражается одновременно "
    "по нескольким каналам (вербальный, просодический, визуальный), и отдельно "
    "взятая модальность часто недостаточна для надежной классификации. "
    "Мультимодальный подход позволяет объединить комплементарные признаки из "
    "разных каналов, повышая качество распознавания."
)

# ── Обзор предметной области ──────────────────────────────────────────────────

doc.add_heading("Обзор предметной области", level=1)

doc.add_heading("Предмет изучения", level=2)

doc.add_paragraph(
    "A/H проявляется как одновременное присутствие положительных и отрицательных "
    "установок к одному и тому же объекту или действию. В видеозаписях это может "
    "выражаться в несогласованности между содержанием речи, интонацией и жестами. "
    "Задача формулируется как бинарная классификация: для каждого видео определить, "
    "присутствует ли A/H (класс 1) или нет (класс 0)."
)

doc.add_heading("Существующие подходы", level=2)

doc.add_paragraph(
    "В области мультимодального анализа аффектов применяются два основных подхода "
    "к объединению модальностей:"
)

doc.add_paragraph(
    "Раннее слияние (early fusion) --- конкатенация эмбеддингов от всех модальностей "
    "в один вектор с последующей подачей в общий классификатор. "
    "Преимущество: модель может обучаться межмодальным корреляциям. "
    "Недостаток: высокая размерность объединенного вектора и необходимость "
    "единого набора гиперпараметров для всех модальностей.",
    style="List Bullet",
)

doc.add_paragraph(
    "Позднее слияние (late fusion) --- обучение отдельного классификатора для каждой "
    "модальности с последующим объединением их вероятностных предсказаний. "
    "Преимущество: возможность настройки архитектуры и гиперпараметров под каждую "
    "модальность независимо. Недостаток: потеря прямых межмодальных зависимостей.",
    style="List Bullet",
)

doc.add_paragraph(
    "В данной работе реализованы оба подхода. Основной упор сделан на позднее слияние, "
    "поскольку оно позволяет гибко подбирать архитектуру MLP-головы и параметры обучения "
    "для каждой модальности."
)

# ── Постановка задачи ─────────────────────────────────────────────────────────

doc.add_heading("Постановка задачи", level=1)

doc.add_heading("Входные и выходные данные", level=2)

doc.add_paragraph(
    "Вход: видеозапись длительностью от нескольких секунд до нескольких минут, "
    "содержащая речь и изображение человека. Из видео извлекаются три модальности: "
    "транскрипт речи (текст), аудиодорожка (голос), последовательность кадров (видео)."
)

doc.add_paragraph(
    "Выход: бинарная метка (0 --- A/H отсутствует, 1 --- A/H присутствует) "
    "и вероятности по каждому классу."
)

doc.add_heading("Данные", level=2)

doc.add_paragraph(
    "Используется датасет BAH (Behavioral Ambivalence/Hesitancy), содержащий "
    "около 1500 видеозаписей с бинарной разметкой. Данные разделены на обучающую, "
    "валидационную и тестовую выборки. Помимо исходных видеофайлов, датасет включает "
    "предварительно извлеченные аудиофайлы, транскрипции и покадровые разметки."
)

doc.add_paragraph(
    "Структура данных на диске: директория Videos/ содержит исходные видео (.mp4), "
    "Frames/ --- извлеченные кадры, SegmentedFrames/ --- кадры после сегментации "
    "YOLO (выделение силуэта человека), audio/ --- аудиодорожки (.wav), "
    "transcription/ --- транскрипции в формате YAML, split/ --- файлы разбиения "
    "на train/val/test."
)

doc.add_heading("Ограничения", level=2)

doc.add_paragraph(
    "Относительно небольшой размер датасета (~1500 записей) ограничивает "
    "сложность обучаемых моделей и требует применения методов регуляризации "
    "(dropout, label smoothing, L2-нормализация, early stopping)."
)

# ── Выбор метода ──────────────────────────────────────────────────────────────

doc.add_heading("Выбор метода", level=1)

doc.add_heading("Архитектура системы", level=2)

doc.add_paragraph(
    "Система состоит из трех этапов: (1) дообучение предобученных энкодеров "
    "на датасете BAH, (2) извлечение эмбеддингов фиксированной размерности, "
    "(3) обучение классификационных голов и объединение их предсказаний."
)

doc.add_heading("Текстовая модальность", level=2)

doc.add_paragraph(
    "Транскрипт речи обрабатывается предобученным языковым трансформером. "
    "Доступны два взаимозаменяемых энкодера:"
)

doc.add_paragraph(
    "DistilBERT (distilbert-base-uncased) --- 6-слойный трансформер с 66 млн "
    "параметров. Извлекает CLS-эмбеддинг размерности 768.",
    style="List Bullet",
)

doc.add_paragraph(
    "DistilRoBERTa-Emotional (j-hartmann/emotion-english-distilroberta-base) --- "
    "6-слойный трансформер с 82 млн параметров, предобученный на задаче классификации "
    "эмоций по 7 категориям. Также выдает эмбеддинг размерности 768.",
    style="List Bullet",
)

doc.add_paragraph(
    "Энкодер дообучается на BAH для бинарной классификации A/H. После дообучения "
    "веса замораживаются, и модель используется только для извлечения эмбеддингов. "
    "При извлечении тренировочных эмбеддингов применяется аугментация текста: "
    "случайная замена слов на токен [MASK] и случайное удаление слов."
)

doc.add_heading("Аудио модальность", level=2)

doc.add_paragraph(
    "Аудиодорожка (16 кГц, до 30 секунд) обрабатывается предобученным "
    "аудио-трансформером. Доступны два варианта:"
)

doc.add_paragraph(
    "Wav2Vec2-Emotional (ehcalabres/wav2vec2-lg-xlsr-en-speech-emotion-recognition) "
    "--- модель на базе XLSR-Large, предобученная на распознавании эмоций "
    "в английской речи. Выходной проецированный эмбеддинг имеет размерность 256.",
    style="List Bullet",
)

doc.add_paragraph(
    "HuBERT (superb/hubert-large-superb-er) --- модель HuBERT-Large, "
    "предобученная на распознавании эмоций через бенчмарк SUPERB. "
    "Также выдает эмбеддинг размерности 256.",
    style="List Bullet",
)

doc.add_paragraph(
    "Аугментация аудио (изменение громкости, аддитивный шум, временной сдвиг, "
    "маскирование участков тишиной) доступна, но по умолчанию отключена, "
    "так как замороженные аудио-энкодеры показывают снижение качества "
    "на аугментированных сигналах."
)

doc.add_heading("Видео модальность", level=2)

doc.add_paragraph(
    "Перед извлечением видео-эмбеддингов кадры проходят через модель "
    "сегментации YOLO11x-seg, которая выделяет силуэт человека и удаляет фон. "
    "Это снижает влияние посторонних объектов и направляет внимание модели "
    "на язык тела. Кадры, на которых человек не обнаружен, помечаются как пропущенные."
)

doc.add_paragraph("Доступны два видео-энкодера:")

doc.add_paragraph(
    "Swin-Tiny (microsoft/swin-tiny-patch4-window7-224) --- визуальный трансформер, "
    "работающий с клипами по 32 кадра. Средний пулинг по временной оси дает "
    "эмбеддинг размерности 768.",
    style="List Bullet",
)

doc.add_paragraph(
    "VideoMAE (MCG-NJU/videomae-base) --- маскированный автоэнкодер для видео, "
    "работающий с клипами по 16 кадров. Средний пулинг по временной оси "
    "также дает эмбеддинг размерности 768.",
    style="List Bullet",
)

doc.add_paragraph(
    "При извлечении тренировочных эмбеддингов применяется аугментация: "
    "горизонтальное отражение, джиттер яркости/контраста, замена отдельных кадров "
    "соседними. Для каждого тренировочного образца создается несколько аугментированных "
    "видов (по умолчанию 5), что увеличивает разнообразие данных без повторного "
    "запуска энкодера на каждой эпохе."
)

# ── Исследование ──────────────────────────────────────────────────────────────

doc.add_heading("Исследование", level=1)

doc.add_heading("Извлечение эмбеддингов", level=2)

doc.add_paragraph(
    "Каждый дообученный энкодер преобразует входные данные в вектор фиксированной "
    "размерности. Для тренировочной выборки извлекается несколько аугментированных "
    "видов (multi-view extraction), для валидационной и тестовой --- по одному "
    "детерминированному виду. Результаты кешируются в формате PyTorch-тензоров "
    "(.pt) в директории multimodal/cache/."
)

doc.add_heading("Раннее слияние (baseline)", level=2)

doc.add_paragraph(
    "В качестве базового решения реализовано раннее слияние: "
    "нормализация каждой модальности (StandardScaler), конкатенация в один вектор, "
    "опциональное снижение размерности через PCA, затем классификация одним из "
    "алгоритмов: MLP, SVM, Random Forest или градиентный бустинг (XGBoost/CatBoost)."
)

doc.add_heading("Позднее слияние (основной подход)", level=2)

doc.add_paragraph(
    "Для каждой модальности обучается отдельная MLP-голова (ModalityMLP). "
    "Архитектура головы настраивается независимо для каждой модальности и включает "
    "следующие компоненты:"
)

doc.add_paragraph(
    "L2-нормализация входного эмбеддинга (поэлементное деление вектора на его "
    "L2-норму) вместо StandardScaler.",
    style="List Bullet",
)
doc.add_paragraph(
    "Опциональный гауссов шум на входе (Gaussian Noise) --- действует только "
    "при обучении, выступая в роли дополнительной регуляризации.",
    style="List Bullet",
)
doc.add_paragraph(
    "Скрытые полносвязные слои с настраиваемыми размерностями (hidden_dims). "
    "Пустой список означает прямой линейный классификатор.",
    style="List Bullet",
)
doc.add_paragraph(
    "BatchNorm после каждого скрытого линейного слоя (до функции активации).",
    style="List Bullet",
)
doc.add_paragraph(
    "Функция активации: ReLU или GELU.",
    style="List Bullet",
)
doc.add_paragraph(
    "Dropout после активации в каждом скрытом слое.",
    style="List Bullet",
)

doc.add_paragraph(
    "Обучение проводится оптимизатором AdamW с планировщиком скорости обучения "
    "(CosineAnnealingLR или ReduceLROnPlateau), ранней остановкой по валидационному "
    "macro-F1, ограничением градиентов (gradient clipping) и сглаживанием меток "
    "(label smoothing)."
)

p = doc.add_paragraph(
    "Текущие настройки для каждой модальности:"
)

# Table with modality configs
table = doc.add_table(rows=4, cols=7, style="Table Grid")
headers = ["Модальность", "hidden_dims", "Нормализация", "Активация",
           "Планировщик", "LR", "Patience"]
for i, h in enumerate(headers):
    cell = table.rows[0].cells[i]
    cell.text = h
    for paragraph in cell.paragraphs:
        for run in paragraph.runs:
            run.bold = True
            run.font.size = Pt(11)

rows_data = [
    ["Текст", "[]", "L2", "ReLU", "plateau", "1e-3", "20"],
    ["Аудио", "[256, 128]", "L2", "ReLU", "plateau", "5e-4", "20"],
    ["Видео", "[256, 128]", "L2", "ReLU", "plateau", "1e-4", "20"],
]
for r_idx, row_data in enumerate(rows_data):
    for c_idx, val in enumerate(row_data):
        cell = table.rows[r_idx + 1].cells[c_idx]
        cell.text = val
        for paragraph in cell.paragraphs:
            for run in paragraph.runs:
                run.font.size = Pt(11)

doc.add_paragraph()

doc.add_heading("Объединение предсказаний (PSO-оптимизация)", level=2)

doc.add_paragraph(
    "После обучения MLP-голов их softmax-выходы на валидационной выборке "
    "используются для оптимизации параметров слияния. Для каждой модальности "
    "подбираются два параметра:"
)

doc.add_paragraph(
    "Температура (T) --- скалярный делитель логитов перед softmax, "
    "калибрующий уверенность модели.",
    style="List Bullet",
)
doc.add_paragraph(
    "Вес (w) --- коэффициент вклада данной модальности в итоговое предсказание.",
    style="List Bullet",
)

doc.add_paragraph(
    "Совместный поиск весов и температур проводится методом роевой оптимизации "
    "частиц (Particle Swarm Optimization, PSO) с целевой функцией --- максимизация "
    "macro-F1 на валидационной выборке. Оптимизация выполняется в двух пространствах: "
    "вероятностном (взвешенное среднее softmax-вероятностей) и логитном "
    "(взвешенное среднее логитов с последующим softmax). Лучший из двух вариантов "
    "выбирается по валидационному F1."
)

doc.add_paragraph(
    "Параметры PSO: размер роя --- 40 частиц, число итераций --- 200, "
    "инерция --- 0.7, когнитивное и социальное ускорение --- 1.5, "
    "диапазон температур --- [0.25, 5.0]."
)

doc.add_heading("Воспроизводимость", level=2)

doc.add_paragraph(
    "Зафиксирован seed=42. Все конфигурации хранятся в TOML-файлах. "
    "Обученные веса, нормализаторы и метаданные слияния сохраняются "
    "в директории multimodal/weights/."
)

# ── Метрики и оценки ──────────────────────────────────────────────────────────

doc.add_heading("Метрики и оценки", level=1)

doc.add_heading("Технические метрики", level=2)

doc.add_paragraph(
    "Основная метрика --- macro-F1, усредненная по двум классам. "
    "Выбор обусловлен несбалансированностью классов в датасете: accuracy "
    "не отражает качество распознавания миноритарного класса."
)

doc.add_paragraph(
    "Дополнительно отслеживаются: accuracy, precision, recall и полный "
    "classification report (по валидационной и тестовой выборкам)."
)

doc.add_heading("Оценка модальностей", level=2)

doc.add_paragraph(
    "Каждая MLP-голова оценивается по отдельности на валидационной и тестовой "
    "выборках. Далее оцениваются все комбинации подмножеств модальностей "
    "(text+audio, text+video, audio+video, text+audio+video) с разными "
    "стратегиями слияния: среднее, взвешенное среднее (PSO-веса), "
    "логитное слияние и мета-стекинг."
)

# ── Демо-приложение ───────────────────────────────────────────────────────────

doc.add_heading("Демо-приложение", level=1)

doc.add_paragraph(
    "Для демонстрации работы системы разработано GUI-приложение на базе Tkinter. "
    "Приложение открывает веб-камеру, записывает видео и аудио одновременно, "
    "после чего запускает полный пайплайн:"
)

doc.add_paragraph("Извлечение или использование записанного аудио.", style="List Number")
doc.add_paragraph("Транскрибирование речи через OpenAI Whisper.", style="List Number")
doc.add_paragraph("Сегментация кадров через YOLO (выделение человека).", style="List Number")
doc.add_paragraph(
    "Извлечение текстового, аудио- и видео-эмбеддингов "
    "дообученными энкодерами.",
    style="List Number",
)
doc.add_paragraph(
    "Классификация через MLP-головы и PSO-оптимизированное "
    "взвешенное слияние.",
    style="List Number",
)
doc.add_paragraph("Отображение результата в интерфейсе.", style="List Number")

doc.add_paragraph(
    "Интерфейс содержит область предпросмотра с камеры, кнопку записи "
    "и боковую панель с журналом обработки. Все сообщения журнала выводятся "
    "на русском языке."
)

# ── Выводы ────────────────────────────────────────────────────────────────────

doc.add_heading("Выводы", level=1)

doc.add_paragraph(
    "Разработана система мультимодальной классификации A/H, использующая "
    "три канала (текст, аудио, видео) с независимыми предобученными энкодерами "
    "и настраиваемыми MLP-головами. Позднее слияние с PSO-оптимизацией "
    "весов и температур позволяет учитывать разную информативность модальностей."
)

doc.add_paragraph(
    "Система реализована в виде модульного пайплайна: дообучение энкодеров, "
    "кеширование эмбеддингов, обучение классификационных голов и демо-приложение "
    "для работы в реальном времени."
)

doc.add_heading("Перспективы", level=2)

doc.add_paragraph(
    "Увеличение объема данных и применение кросс-валидации для более "
    "надежной оценки обобщающей способности.",
    style="List Number",
)
doc.add_paragraph(
    "Использование механизмов внимания (cross-modal attention) для учета "
    "межмодальных зависимостей при слиянии.",
    style="List Number",
)
doc.add_paragraph(
    "Квантование моделей для ускорения инференса на устройствах без GPU.",
    style="List Number",
)

# ── Save ──────────────────────────────────────────────────────────────────────

doc.save("НИР_описание_проекта.docx")
print("Saved: НИР_описание_проекта.docx")
